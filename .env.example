# MQTT — set MQTT_ENABLED=true when Mosquitto is running
MQTT_ENABLED=false
MQTT_BROKER=localhost
MQTT_PORT=1883

# API server (HTTP POST — parallel path alongside MQTT)
INGEST_URL=http://localhost:5050/api/ingest
